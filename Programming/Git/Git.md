После команды `git init` создается структура каталогов в дирректории `.git`, необходимых для функционирования `Git`

```shell
ls .git/
branches  config  description  HEAD  hooks  info  objects  refs
```

Из них наиболее важными являются:
- `objects`
- `refs`
- `HEAD`




